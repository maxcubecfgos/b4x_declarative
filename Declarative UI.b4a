Build1=Default,b4a.example
Group=Default Group
Library1=core
Library2=xui
ManifestCode='This code will be applied to the manifest file during compilation.~\n~'You do not need to modify it in most cases.~\n~'See this link for for more information: https://www.b4x.com/forum/showthread.php?p=78136~\n~AddManifestText(~\n~<uses-sdk android:minSdkVersion="21" android:targetSdkVersion="35"/>~\n~<supports-screens android:largeScreens="true" ~\n~    android:normalScreens="true" ~\n~    android:smallScreens="true" ~\n~    android:anyDensity="true"/>)~\n~SetApplicationAttribute(android:icon, "@drawable/icon")~\n~SetApplicationAttribute(android:label, "$LABEL$")~\n~CreateResourceFromFile(Macro, Themes.LightTheme)~\n~'End of default text.~\n~
Module1=Starter
Module10=UIFloatingActionButton
Module11=UILabel
Module12=UIPadding
Module13=UIRow
Module14=UIScaffold
Module15=UISpace
Module2=UIAppBar
Module3=UIBox
Module4=UIButton
Module5=UICard
Module6=UICenter
Module7=UIColumn
Module8=UIDivider
Module9=UIExpanded
NumberOfFiles=0
NumberOfLibraries=2
NumberOfModules=15
Version=13.5
@EndOfDesignText@
#Region  Project Attributes 
    #ApplicationLabel: B4A Declarative UI
    #VersionCode: 1
    #VersionName: 
    #SupportedOrientations: unspecified
    #CanInstallToExternalStorage: False
#End Region

#Region  Activity Attributes 
    #FullScreen: False
    #IncludeTitle: False
#End Region

Sub Process_Globals
	Private Counter As Int = 0
End Sub

Sub Globals
	Private Root As B4XView
    
	' Structural Widgets
	Private AppAppBar As UIAppBar
	Private FabDecrement As UIFloatingActionButton
	Private FabIncrement As UIFloatingActionButton
    
	' Atomic Widgets
	Private LblTitle As UILabel
	Private LblValue As UILabel
	Private MyDivider As UIDivider
    
	' Containers
	Private ValueCenter As UICenter ' Centrador para evitar cortes
	Private ColumnCardBody As UIColumn
	Private PaddingCard As UIPadding
	Private MainCard As UICard
    
	' Root Scaffold
	Private PaddingScreen As UIPadding
	Private AppScaffold As UIScaffold
End Sub

Sub Activity_Create(FirstTime As Boolean)
	Root = Activity
	Root.Color = 0xFFF5F5F5
    
	' 1. Initialize Components
	AppAppBar.Initialize _
        .Title("Declarative Dashboard") _
        .BackgroundColor(0xFF1976D2)
        
	FabDecrement.Initialize.Text("-").BackgroundColor(0xFFFF5252).OnClick(Me, "Decrement_Click")
	FabIncrement.Initialize.Text("+").BackgroundColor(0xFF448AFF).OnClick(Me, "Increment_Click")
        
	LblTitle.Initialize.Text("SYSTEM STATUS").Size(14).Color(0xFF607D8B)
	LblValue.Initialize.Size(96).Color(0xFF212121)
    
	MyDivider.Initialize
	ValueCenter.Initialize.Child(LblValue)
    
	' 2. Build Card Tree
	ColumnCardBody.Initialize _
        .AddChild(LblTitle) _
        .AddChild(MyDivider) _
        .AddChild(ValueCenter)
        
	PaddingCard.Initialize.All(24dip).Child(ColumnCardBody)
    
	MainCard.Initialize _
        .BackgroundColor(Colors.White) _
        .CornerRadius(16dip) _
        .Child(PaddingCard)
        
	' 3. Scaffold Assembly
	PaddingScreen.Initialize.All(20dip).Child(MainCard)
    
	AppScaffold.Initialize _
        .AppBar(AppAppBar) _
        .Body(PaddingScreen) _
        .FloatingActionButtonLeft(FabDecrement) _
        .FloatingActionButtonRight(FabIncrement)
        
	BuildUI
End Sub

Private Sub SetState(NewCounterValue As Int)
	Counter = NewCounterValue
	BuildUI
End Sub

Private Sub BuildUI
	LblValue.Text("" & Counter)
    
	' Reactive Color Logic
	If Counter < 0 Then
		LblValue.Color(0xFFD32F2F)
	Else
		LblValue.Color(0xFF212121)
	End If
    
	' Render
	AppScaffold.Render(Root, 0, 0, Root.Width, Root.Height)
End Sub

' --- Event Handlers ---
Sub Increment_Click
	SetState(Counter + 1)
End Sub

Sub Decrement_Click
	SetState(Counter - 1)
End Sub